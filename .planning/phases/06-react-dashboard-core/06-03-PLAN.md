---
phase: 06-react-dashboard-core
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - dashboard/src/components/charts/RoiBarChart.tsx
  - dashboard/src/components/shared/FilterBar.tsx
  - dashboard/src/components/shared/FilterChip.tsx
  - dashboard/src/components/shared/BetHistoryTable.tsx
  - dashboard/src/tabs/BacktestTab.tsx
  - dashboard/src/__tests__/RoiBarChart.test.tsx
  - dashboard/src/__tests__/BacktestTab.test.tsx
autonomous: true
requirements:
  - DASH-02

must_haves:
  truths:
    - "User sees 5 ROI bar charts on the Backtest tab: Surface, Tournament Level, Year, EV Bucket, Rank Tier"
    - "Clicking a bar segment filters the bet history table to matching rows"
    - "Active chart filter appears as a dismissible chip above the table"
    - "User can filter by surface, year, and model via dropdown filter bar"
    - "Bet history table is paginated with 20 rows per page"
    - "Positive ROI values are green, negative are red in both charts and table"
  artifacts:
    - path: "dashboard/src/components/charts/RoiBarChart.tsx"
      provides: "Reusable Nivo horizontal bar chart with click handler"
      contains: "ResponsiveBar"
    - path: "dashboard/src/components/shared/BetHistoryTable.tsx"
      provides: "Paginated table of backtest bets"
      contains: "Showing"
    - path: "dashboard/src/tabs/BacktestTab.tsx"
      provides: "Full backtest tab with charts, filters, table"
      contains: "chartFilter"
  key_links:
    - from: "dashboard/src/tabs/BacktestTab.tsx"
      to: "dashboard/src/components/charts/RoiBarChart.tsx"
      via: "onBarClick callback sets chartFilter state"
      pattern: "setChartFilter"
    - from: "dashboard/src/tabs/BacktestTab.tsx"
      to: "dashboard/src/components/shared/BetHistoryTable.tsx"
      via: "passes chartFilter as filter prop"
      pattern: "filter.*chartFilter"
    - from: "dashboard/src/tabs/BacktestTab.tsx"
      to: "dashboard/src/hooks/useBacktest.ts"
      via: "useBacktestSummary and useBacktestBets hooks"
      pattern: "useBacktest"
---

<objective>
Build the Backtest tab with 5 ROI bar charts (surface, tournament level, year, EV bucket, rank tier), filter bar with dropdowns, paginated bet history table, and click-to-filter interaction between charts and table.

Purpose: The Backtest tab is the primary analytics view — it lets users explore ROI breakdowns across multiple dimensions and drill into individual bets via chart-click filtering.

Output: Fully interactive Backtest tab with bar chart click-to-filter, dropdown filtering, paginated bet table, and tests.
</objective>

<execution_context>
@C:/Users/regis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/regis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-react-dashboard-core/06-CONTEXT.md
@.planning/phases/06-react-dashboard-core/06-RESEARCH.md
@.planning/phases/06-react-dashboard-core/06-UI-SPEC.md
@.planning/phases/06-react-dashboard-core/06-01-SUMMARY.md

<interfaces>
From dashboard/src/api/types.ts:
```typescript
export interface BacktestSummary {
  n_bets: number; kelly_roi: number; flat_roi: number;
  total_pnl_kelly: number; total_pnl_flat: number;
  by_surface: Record<string, any>[];
  by_tourney_level: Record<string, any>[];
  by_year: Record<string, any>[];
  by_ev_bucket: Record<string, any>[];
  by_rank_tier: Record<string, any>[];
}
export interface BacktestBetRow {
  id: number; fold_year: number; tourney_id: string; match_num: number;
  tour: string; model_version: string; player_id: number; outcome: number;
  calibrated_prob: number; decimal_odds: number; ev: number;
  kelly_bet: number; pnl_kelly: number; pnl_flat: number;
  bankroll_before: number; bankroll_after: number;
  surface: string | null; tourney_level: string | null; tourney_date: string;
}
export interface PaginatedBetsResponse {
  total: number; offset: number; limit: number; data: BacktestBetRow[];
}
```

From dashboard/src/hooks/useBacktest.ts:
```typescript
export function useBacktestSummary(params?: { surface?: string; year?: string; model?: string }): UseQueryResult<BacktestSummary>;
export function useBacktestBets(offset: number, limit: number, filters?: Record<string, string>): UseQueryResult<PaginatedBetsResponse>;
```

From dashboard/src/components/charts/nivoTheme.ts:
```typescript
export const nivoTheme: { ... };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build RoiBarChart, FilterBar, FilterChip, BetHistoryTable components</name>
  <files>
    dashboard/src/components/charts/RoiBarChart.tsx
    dashboard/src/components/shared/FilterBar.tsx
    dashboard/src/components/shared/FilterChip.tsx
    dashboard/src/components/shared/BetHistoryTable.tsx
  </files>
  <read_first>
    dashboard/src/api/types.ts
    dashboard/src/components/charts/nivoTheme.ts
    .planning/phases/06-react-dashboard-core/06-UI-SPEC.md
    .planning/phases/06-react-dashboard-core/06-RESEARCH.md
  </read_first>
  <action>
    1. Create src/components/charts/RoiBarChart.tsx:
       - Props: { data: { id: string; roi: number }[]; dimension: string; onBarClick: (value: string) => void; activeFilter?: string }
       - Uses Nivo ResponsiveBar with nivoTheme
       - Chart config:
         - keys={['roi']}
         - indexBy="id"
         - layout="horizontal" (labels on left axis, bars horizontal)
         - theme={nivoTheme}
         - colors={(bar) => bar.data.id === activeFilter ? '#60a5fa' : (Number(bar.value) >= 0 ? '#22c55e' : '#ef4444')}
         - onClick={(bar) => onBarClick(String(bar.indexValue))}
         - margin={{ top: 10, right: 20, bottom: 30, left: 80 }}
         - axisLeft={{ tickSize: 0 }}
         - axisBottom={{ legend: 'ROI %', legendPosition: 'middle', legendOffset: 25 }}
         - enableGridY={false}
         - enableLabel={false}
         - Custom tooltip: dark styled div showing dimension value and ROI percentage
       - Container: div style={{ height: 220 }} role="img" aria-label={`ROI by ${dimension}`}

    2. Create src/components/shared/FilterBar.tsx:
       - Props: { filters: { key: string; label: string; options: string[]; value: string; onChange: (val: string) => void }[] }
       - Uses shadcn/ui Select component for each filter
       - Background: bg-slate-800, border-b border-slate-700, h-11, px-6
       - Each filter: label (text-xs uppercase tracking-wider text-slate-400) + Select trigger (bg-slate-800 border-slate-700 text-slate-100)
       - "All" option as first entry in each dropdown (value="")
       - Gap between filters: gap-4 (md)
       - Flex row layout with items-center

    3. Create src/components/shared/FilterChip.tsx:
       - Props: { label: string; onDismiss: () => void }
       - Pill shape: inline-flex items-center bg-slate-700 text-slate-100 text-xs rounded-full px-3 py-1 gap-1
       - Prefix text: "Filtered by:" in text-slate-400
       - X button: lucide-react X icon, 14px, aria-label="Remove {label} filter"
       - onClick calls onDismiss

    4. Create src/components/shared/BetHistoryTable.tsx:
       - Props: { data: BacktestBetRow[]; total: number; offset: number; limit: number; onPageChange: (offset: number) => void; isLoading: boolean }
       - Uses shadcn/ui Table (Table, TableHeader, TableBody, TableRow, TableHead, TableCell)
       - Columns: Date, Surface, Tournament, Model, EV%, Kelly Bet, P&L, Bankroll
       - Date: tourney_date formatted
       - Surface: surface ?? 'N/A'
       - EV%: ev formatted to 2 decimals with % suffix, green if >0, red if <0
       - P&L: pnl_kelly formatted as currency, green-500 if positive, red-500 if negative
       - Header: bg-slate-800, text-xs uppercase tracking-wider text-slate-400
       - Row even/odd: bg-slate-800 / bg-slate-900, hover:bg-slate-700
       - Pagination footer: "Showing {offset+1}--{min(offset+limit, total)} of {total} bets"
       - Prev/Next buttons using shadcn Button variant="outline" size="sm"
       - Prev disabled when offset=0, Next disabled when offset+limit >= total
       - 20 rows per page (limit=20 default)
       - Loading state: show SkeletonCard variant="table"
       - Empty state: "No backtest results" / "Run a backtest to see ROI breakdowns and bet history. Use the API or CLI to trigger a run."
  </action>
  <verify>
    <automated>cd dashboard && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - dashboard/src/components/charts/RoiBarChart.tsx contains ResponsiveBar
    - dashboard/src/components/charts/RoiBarChart.tsx contains onClick.*onBarClick
    - dashboard/src/components/charts/RoiBarChart.tsx contains layout="horizontal"
    - dashboard/src/components/charts/RoiBarChart.tsx contains role="img"
    - dashboard/src/components/shared/FilterBar.tsx contains Select
    - dashboard/src/components/shared/FilterChip.tsx contains aria-label.*Remove
    - dashboard/src/components/shared/FilterChip.tsx contains Filtered by
    - dashboard/src/components/shared/BetHistoryTable.tsx contains TableHeader
    - dashboard/src/components/shared/BetHistoryTable.tsx contains Showing
    - dashboard/src/components/shared/BetHistoryTable.tsx contains pnl_kelly
    - `cd dashboard && npm run build` exits 0
  </acceptance_criteria>
  <done>RoiBarChart with click handler, FilterBar with Select dropdowns, FilterChip with dismiss, BetHistoryTable with pagination — all typed and styled per UI-SPEC</done>
</task>

<task type="auto">
  <name>Task 2: Assemble BacktestTab with click-to-filter state, 5 ROI charts, filter bar, bet table, and tests</name>
  <files>
    dashboard/src/tabs/BacktestTab.tsx
    dashboard/src/__tests__/RoiBarChart.test.tsx
    dashboard/src/__tests__/BacktestTab.test.tsx
  </files>
  <read_first>
    dashboard/src/components/charts/RoiBarChart.tsx
    dashboard/src/components/shared/FilterBar.tsx
    dashboard/src/components/shared/FilterChip.tsx
    dashboard/src/components/shared/BetHistoryTable.tsx
    dashboard/src/hooks/useBacktest.ts
    dashboard/src/api/types.ts
    .planning/phases/06-react-dashboard-core/06-UI-SPEC.md
  </read_first>
  <action>
    1. Build src/tabs/BacktestTab.tsx:
       - State:
         - `chartFilter: { dimension: string; value: string } | null` — set by bar chart clicks
         - `filterParams: { surface: string; year: string; model: string }` — set by filter bar dropdowns
         - `page: number` (starting at 0) — for pagination offset
       - Hooks:
         - useBacktestSummary(filterParams) for summary + breakdowns
         - useBacktestBets(page * 20, 20, buildBetsFilterObject(filterParams, chartFilter)) for paginated bets
       - buildBetsFilterObject: merges filterParams with chartFilter into a flat Record<string, string> for query params
       - Layout (per UI-SPEC Backtest Tab Layout):
         a) FilterBar at top with 3 dropdowns:
            - Surface: ["Hard", "Clay", "Grass", "Carpet"] (from unique values, or hardcoded known surfaces)
            - Year: dynamically from data (extract unique fold_year values from by_year breakdown)
            - Model: dynamically from data or hardcoded ["logistic_v1"]
         b) ROI charts grid: 4 charts in 2x2 grid (lg:grid-cols-2) + 1 full-width below:
            - Surface: data from summary.by_surface, maps to { id: surface_name, roi: kelly_roi }
            - Tournament Level: data from summary.by_tourney_level
            - Year: data from summary.by_year
            - EV Bucket: data from summary.by_ev_bucket
            - Rank Tier: data from summary.by_rank_tier (full width below grid)
            Each chart gets onBarClick={(value) => handleBarClick(dimension, value)}
         c) handleBarClick: toggles chartFilter — click same bar again to clear:
            ```ts
            const handleBarClick = (dimension: string, value: string) => {
              setChartFilter(prev =>
                prev?.dimension === dimension && prev.value === value ? null : { dimension, value }
              );
              setPage(0); // reset pagination on filter change
            };
            ```
         d) FilterChip: shown when chartFilter is set, displays "Filtered by: {dimension}: {value}", dismiss clears chartFilter
         e) BetHistoryTable: paginated, receives data from useBacktestBets
       - Loading: show SkeletonCard for charts when summary isLoading, SkeletonCard variant="table" for table
       - Error: EmptyState with backtest empty state copy from UI-SPEC
       - Reset page to 0 when any filter changes

    2. Create src/__tests__/RoiBarChart.test.tsx:
       - Mock @nivo/bar (ResponsiveBar renders null in jsdom)
       - Test: renders container with role="img" and aria-label containing dimension name
       - Test: renders with positive/negative data without errors

    3. Create src/__tests__/BacktestTab.test.tsx:
       - Mock useBacktestSummary and useBacktestBets to return test data
       - Test: renders filter bar with Surface, Year, Model labels
       - Test: renders 5 chart containers with role="img"
       - Test: shows empty state when no data (backtest empty state copy)
       - Test: shows skeleton loaders when isLoading=true
       - Test: renders bet history table with "Showing" pagination text
  </action>
  <verify>
    <automated>cd dashboard && npx vitest run --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - dashboard/src/tabs/BacktestTab.tsx contains setChartFilter
    - dashboard/src/tabs/BacktestTab.tsx contains handleBarClick
    - dashboard/src/tabs/BacktestTab.tsx contains useBacktestSummary
    - dashboard/src/tabs/BacktestTab.tsx contains useBacktestBets
    - dashboard/src/tabs/BacktestTab.tsx contains FilterBar
    - dashboard/src/tabs/BacktestTab.tsx contains FilterChip
    - dashboard/src/tabs/BacktestTab.tsx contains BetHistoryTable
    - dashboard/src/tabs/BacktestTab.tsx contains RoiBarChart (appears 5 times for 5 dimensions)
    - dashboard/src/tabs/BacktestTab.tsx contains by_surface
    - dashboard/src/__tests__/BacktestTab.test.tsx contains role="img"
    - dashboard/src/__tests__/RoiBarChart.test.tsx contains role="img"
    - `cd dashboard && npx vitest run` exits 0 with all tests passing
  </acceptance_criteria>
  <done>Backtest tab renders 5 ROI bar charts with click-to-filter, filter bar with 3 dropdowns, dismissible filter chip, paginated bet history table (20 rows), and all tests pass</done>
</task>

</tasks>

<verification>
- `cd dashboard && npx vitest run --reporter=verbose` — all Backtest tab tests pass
- `cd dashboard && npm run build` — zero TypeScript errors
- Backtest tab renders 5 ROI charts, filter bar, and paginated table
- Click-to-filter state management works (bar click -> filter -> chip -> dismiss)
</verification>

<success_criteria>
- DASH-02: ROI bar charts render for all 5 dimensions (surface, tournament level, year, EV bucket, rank tier)
- Bar charts use horizontal layout with positive=green, negative=red
- Click-to-filter interaction: clicking bar sets chart filter, shown as dismissible chip, filters bet table
- Filter bar dropdowns for surface, year, model
- Paginated bet table with 20 rows/page, prev/next navigation
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-react-dashboard-core/06-03-SUMMARY.md`
</output>
