---
phase: 06-react-dashboard-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/package.json
  - dashboard/vite.config.ts
  - dashboard/tsconfig.json
  - dashboard/tsconfig.app.json
  - dashboard/tsconfig.node.json
  - dashboard/tailwind.config.js
  - dashboard/postcss.config.js
  - dashboard/index.html
  - dashboard/components.json
  - dashboard/src/main.tsx
  - dashboard/src/App.tsx
  - dashboard/src/index.css
  - dashboard/src/api/client.ts
  - dashboard/src/api/types.ts
  - dashboard/src/providers/QueryProvider.tsx
  - dashboard/src/components/layout/Header.tsx
  - dashboard/src/components/layout/TabNav.tsx
  - dashboard/src/components/charts/nivoTheme.ts
  - dashboard/src/tabs/OverviewTab.tsx
  - dashboard/src/tabs/BacktestTab.tsx
  - dashboard/src/tabs/ModelsTab.tsx
  - dashboard/src/tabs/SignalsTab.tsx
  - dashboard/vitest.setup.ts
  - dashboard/src/__tests__/App.test.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - DASH-06

must_haves:
  truths:
    - "Running `npm run dev` in dashboard/ starts Vite dev server on port 5173"
    - "Browser shows dark-themed page with 4 tabs: Overview, Backtest, Models, Signals"
    - "Clicking each tab renders a placeholder panel with the tab name"
    - "TypeScript types match all Pydantic schemas from src/api/schemas.py"
    - "API client fetches from http://localhost:8000/api/v1 base URL"
    - "Vitest runs successfully with at least one passing test"
  artifacts:
    - path: "dashboard/package.json"
      provides: "All locked dependencies installed"
      contains: "tailwindcss"
    - path: "dashboard/src/api/types.ts"
      provides: "TypeScript interfaces for all API response types"
      exports: ["BankrollResponse", "BacktestSummary", "PredictionRow", "ModelMetrics", "CalibrationResponse"]
    - path: "dashboard/src/api/client.ts"
      provides: "Typed fetch wrapper"
      exports: ["apiFetch"]
    - path: "dashboard/src/App.tsx"
      provides: "App shell with Header + TabNav + 4 tab panels"
      contains: "Overview"
  key_links:
    - from: "dashboard/src/App.tsx"
      to: "dashboard/src/providers/QueryProvider.tsx"
      via: "QueryProvider wraps App content"
      pattern: "QueryProvider"
    - from: "dashboard/src/api/client.ts"
      to: "http://localhost:8000/api/v1"
      via: "BASE_URL constant"
      pattern: "localhost:8000/api/v1"
---

<objective>
Scaffold the React dashboard project with Vite + TypeScript, install all locked dependencies (Tailwind v3, shadcn/ui, TanStack Query v5, Nivo, Lightweight Charts), configure the dark theme, create the API client layer with TypeScript types, build the app shell (Header + 4-tab navigation), and set up test infrastructure.

Purpose: Establishes the foundation that all subsequent plans build on — no chart or feature plan can run without the project scaffold, dependency installation, API types, and app shell.

Output: A running Vite dev server showing a dark-themed dashboard with 4 navigable tabs (placeholder content) and a working test suite.
</objective>

<execution_context>
@C:/Users/regis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/regis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-react-dashboard-core/06-CONTEXT.md
@.planning/phases/06-react-dashboard-core/06-RESEARCH.md
@.planning/phases/06-react-dashboard-core/06-UI-SPEC.md

<interfaces>
<!-- Source of truth: src/api/schemas.py — all Pydantic v2 models defining exact data shapes -->
<!-- The TypeScript types in this plan MUST mirror these models exactly -->

From src/api/schemas.py:
- PredictionRow: tourney_id(str), match_num(int), tour(str), player_id(int), model_version(str), calibrated_prob(float|null), ev_value(float|null), edge(float|null), decimal_odds(float|null), predicted_at(str)
- PredictResponse: { data: PredictionRow[] }
- BacktestSummary: n_bets(int), kelly_roi(float), flat_roi(float), total_pnl_kelly(float), total_pnl_flat(float), by_surface(dict[]), by_tourney_level(dict[]), by_year(dict[]), by_ev_bucket(dict[]), by_rank_tier(dict[])
- BacktestBetRow: id(int), fold_year(int), tourney_id(str), match_num(int), tour(str), model_version(str), player_id(int), outcome(int), calibrated_prob(float), decimal_odds(float), ev(float), kelly_bet(float), pnl_kelly(float), pnl_flat(float), bankroll_before(float), bankroll_after(float), surface(str|null), tourney_level(str|null), tourney_date(str)
- PaginatedBetsResponse: { total: int, offset: int, limit: int, data: BacktestBetRow[] }
- BankrollPoint: { date: str, bankroll: float }
- BankrollResponse: { initial: float, current: float, peak: float, max_drawdown: float, curve: BankrollPoint[] }
- ModelMetrics: model_version(str), brier_score(float|null), log_loss(float|null), calibration_quality(str|null), kelly_roi(float|null), flat_roi(float|null), total_bets(int)
- ModelsResponse: { data: ModelMetrics[] }
- CalibrationBin: { midpoint: float, empirical_freq: float, n_samples: int }
- CalibrationResponse: { model_version: str, fold: str|null, bins: CalibrationBin[] }
- JobResponse: { job_id: str, status: str }
- RefreshStatusResponse: { job_id: str, status: str, step: str|null, started_at: str|null, result: dict|null }
- ErrorResponse: { error: str, message: str, detail: str|null }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite project, install all dependencies, configure Tailwind v3 + shadcn/ui dark theme + test infra</name>
  <files>
    dashboard/package.json
    dashboard/vite.config.ts
    dashboard/tsconfig.json
    dashboard/tsconfig.app.json
    dashboard/tsconfig.node.json
    dashboard/tailwind.config.js
    dashboard/postcss.config.js
    dashboard/index.html
    dashboard/components.json
    dashboard/src/main.tsx
    dashboard/src/index.css
    dashboard/vitest.setup.ts
  </files>
  <read_first>
    .planning/phases/06-react-dashboard-core/06-RESEARCH.md
    .planning/phases/06-react-dashboard-core/06-UI-SPEC.md
    src/api/schemas.py
  </read_first>
  <action>
    1. From project root (D:/GitRepo/sports-bet-quant-model), scaffold Vite project:
       ```
       npm create vite@latest dashboard -- --template react-ts
       ```

    2. cd into dashboard/ and install Tailwind v3 (MUST pin @3 to avoid v4):
       ```
       npm install -D tailwindcss@3 postcss autoprefixer
       npx tailwindcss init -p
       ```

    3. Configure tailwind.config.js:
       ```js
       /** @type {import('tailwindcss').Config} */
       module.exports = {
         darkMode: 'class',
         content: ['./index.html', './src/**/*.{ts,tsx}'],
         theme: { extend: {} },
         plugins: [],
       };
       ```

    4. Initialize shadcn/ui (use @2.3.0 for Tailwind v3 compatibility):
       ```
       npx shadcn@2.3.0 init
       ```
       When prompted: Style=Default, Base color=Slate, CSS variables=yes.
       If init overwrites tailwind.config.js, restore the darkMode: 'class' setting.

    5. Install shadcn/ui components:
       ```
       npx shadcn@2.3.0 add table tabs card badge button select skeleton popover sonner
       ```

    6. Install remaining dependencies:
       ```
       npm install @tanstack/react-query lightweight-charts @nivo/core @nivo/bar @nivo/scatterplot lucide-react
       npm install -D vitest @vitest/ui jsdom @testing-library/react @testing-library/jest-dom @testing-library/user-event
       ```

    7. Configure src/index.css with dark-only CSS variables (from UI-SPEC):
       ```css
       @tailwind base;
       @tailwind components;
       @tailwind utilities;

       @layer base {
         :root {
           --background: 222.2 84% 4.9%;
           --foreground: 210 40% 98%;
           --card: 222.2 84% 4.9%;
           --card-foreground: 210 40% 98%;
           --popover: 222.2 84% 4.9%;
           --popover-foreground: 210 40% 98%;
           --primary: 142.1 76.2% 36.3%;
           --primary-foreground: 355.7 100% 97.3%;
           --secondary: 217.2 32.6% 17.5%;
           --secondary-foreground: 210 40% 98%;
           --muted: 217.2 32.6% 17.5%;
           --muted-foreground: 215 20.2% 65.1%;
           --accent: 217.2 32.6% 17.5%;
           --accent-foreground: 210 40% 98%;
           --destructive: 0 62.8% 30.6%;
           --destructive-foreground: 210 40% 98%;
           --border: 217.2 32.6% 17.5%;
           --input: 217.2 32.6% 17.5%;
           --ring: 142.1 76.2% 36.3%;
           --radius: 0.5rem;
         }
       }

       body {
         font-family: 'Inter', ui-sans-serif, system-ui, sans-serif;
       }
       ```

    8. Configure src/main.tsx to force dark mode:
       ```tsx
       import React from 'react';
       import ReactDOM from 'react-dom/client';
       import App from './App';
       import './index.css';

       document.documentElement.classList.add('dark');

       ReactDOM.createRoot(document.getElementById('root')!).render(
         <React.StrictMode>
           <App />
         </React.StrictMode>
       );
       ```

    9. Add test configuration to vite.config.ts:
       ```ts
       import { defineConfig } from 'vite';
       import react from '@vitejs/plugin-react';
       import path from 'path';

       export default defineConfig({
         plugins: [react()],
         resolve: {
           alias: {
             '@': path.resolve(__dirname, './src'),
           },
         },
         test: {
           globals: true,
           environment: 'jsdom',
           setupFiles: './vitest.setup.ts',
           css: true,
         },
       });
       ```

    10. Create vitest.setup.ts:
        ```ts
        import '@testing-library/jest-dom';

        // Mock ResizeObserver for chart components
        global.ResizeObserver = class ResizeObserver {
          observe() {}
          unobserve() {}
          disconnect() {}
        };

        // Mock canvas getContext for Lightweight Charts
        HTMLCanvasElement.prototype.getContext = (() => {
          return {
            fillRect: () => {},
            clearRect: () => {},
            getImageData: () => ({ data: [] }),
            putImageData: () => {},
            createImageData: () => [],
            setTransform: () => {},
            drawImage: () => {},
            save: () => {},
            fillText: () => {},
            restore: () => {},
            beginPath: () => {},
            moveTo: () => {},
            lineTo: () => {},
            closePath: () => {},
            stroke: () => {},
            translate: () => {},
            scale: () => {},
            rotate: () => {},
            arc: () => {},
            fill: () => {},
            measureText: () => ({ width: 0 }),
            transform: () => {},
            rect: () => {},
            clip: () => {},
            canvas: { width: 800, height: 600 },
          };
        }) as any;
        ```

    11. Remove Vite boilerplate: delete src/App.css, src/assets/ (if created).
  </action>
  <verify>
    <automated>cd dashboard && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - dashboard/package.json contains "tailwindcss": "^3
    - dashboard/package.json contains "@tanstack/react-query"
    - dashboard/package.json contains "lightweight-charts"
    - dashboard/package.json contains "@nivo/bar"
    - dashboard/package.json contains "@nivo/scatterplot"
    - dashboard/package.json contains "vitest"
    - dashboard/package.json contains "@testing-library/react"
    - dashboard/tailwind.config.js contains darkMode: 'class' (or darkMode: "class")
    - dashboard/src/index.css contains --background: 222.2 84% 4.9%
    - dashboard/src/index.css contains --primary: 142.1 76.2% 36.3%
    - dashboard/src/main.tsx contains document.documentElement.classList.add('dark')
    - dashboard/vite.config.ts contains environment: 'jsdom'
    - dashboard/vitest.setup.ts contains ResizeObserver
    - dashboard/vitest.setup.ts contains getContext
    - dashboard/components.json exists (shadcn/ui initialized)
    - `cd dashboard && npm run build` exits 0
  </acceptance_criteria>
  <done>Vite project scaffolded with all locked dependencies, Tailwind v3 dark theme configured, shadcn/ui initialized with 9 components, test infrastructure ready</done>
</task>

<task type="auto">
  <name>Task 2: Create API client, TypeScript types, QueryProvider, Nivo theme, app shell with Header + TabNav + 4 tab stubs, smoke test</name>
  <files>
    dashboard/src/api/client.ts
    dashboard/src/api/types.ts
    dashboard/src/providers/QueryProvider.tsx
    dashboard/src/components/charts/nivoTheme.ts
    dashboard/src/components/layout/Header.tsx
    dashboard/src/components/layout/TabNav.tsx
    dashboard/src/tabs/OverviewTab.tsx
    dashboard/src/tabs/BacktestTab.tsx
    dashboard/src/tabs/ModelsTab.tsx
    dashboard/src/tabs/SignalsTab.tsx
    dashboard/src/App.tsx
    dashboard/src/main.tsx
    dashboard/src/__tests__/App.test.tsx
  </files>
  <read_first>
    src/api/schemas.py
    .planning/phases/06-react-dashboard-core/06-RESEARCH.md
    .planning/phases/06-react-dashboard-core/06-UI-SPEC.md
    dashboard/src/main.tsx
    dashboard/src/index.css
  </read_first>
  <action>
    1. Create src/api/client.ts — typed fetch wrapper:
       ```ts
       const BASE_URL = 'http://localhost:8000/api/v1';

       export async function apiFetch<T>(path: string, init?: RequestInit): Promise<T> {
         const res = await fetch(`${BASE_URL}${path}`, init);
         if (!res.ok) {
           const err = await res.json().catch(() => ({}));
           throw new Error(err.message ?? `HTTP ${res.status}`);
         }
         return res.json() as Promise<T>;
       }
       ```

    2. Create src/api/types.ts — TypeScript interfaces mirroring src/api/schemas.py exactly:
       ```ts
       // Prediction schemas
       export interface PredictionRow {
         tourney_id: string;
         match_num: number;
         tour: string;
         player_id: number;
         model_version: string;
         calibrated_prob: number | null;
         ev_value: number | null;
         edge: number | null;
         decimal_odds: number | null;
         predicted_at: string;
       }
       export interface PredictResponse { data: PredictionRow[]; }

       // Backtest schemas
       export interface BacktestSummary {
         n_bets: number;
         kelly_roi: number;
         flat_roi: number;
         total_pnl_kelly: number;
         total_pnl_flat: number;
         by_surface: Record<string, any>[];
         by_tourney_level: Record<string, any>[];
         by_year: Record<string, any>[];
         by_ev_bucket: Record<string, any>[];
         by_rank_tier: Record<string, any>[];
       }
       export interface BacktestBetRow {
         id: number;
         fold_year: number;
         tourney_id: string;
         match_num: number;
         tour: string;
         model_version: string;
         player_id: number;
         outcome: number;
         calibrated_prob: number;
         decimal_odds: number;
         ev: number;
         kelly_bet: number;
         pnl_kelly: number;
         pnl_flat: number;
         bankroll_before: number;
         bankroll_after: number;
         surface: string | null;
         tourney_level: string | null;
         tourney_date: string;
       }
       export interface PaginatedBetsResponse {
         total: number;
         offset: number;
         limit: number;
         data: BacktestBetRow[];
       }

       // Bankroll schemas
       export interface BankrollPoint { date: string; bankroll: number; }
       export interface BankrollResponse {
         initial: number;
         current: number;
         peak: number;
         max_drawdown: number;
         curve: BankrollPoint[];
       }

       // Model schemas
       export interface ModelMetrics {
         model_version: string;
         brier_score: number | null;
         log_loss: number | null;
         calibration_quality: string | null;
         kelly_roi: number | null;
         flat_roi: number | null;
         total_bets: number;
       }
       export interface ModelsResponse { data: ModelMetrics[]; }

       // Calibration schemas
       export interface CalibrationBin { midpoint: number; empirical_freq: number; n_samples: number; }
       export interface CalibrationResponse {
         model_version: string;
         fold: string | null;
         bins: CalibrationBin[];
       }

       // Job/refresh schemas
       export interface JobResponse { job_id: string; status: string; }
       export interface RefreshStatusResponse {
         job_id: string;
         status: string;
         step: string | null;
         started_at: string | null;
         result: Record<string, any> | null;
       }
       ```

    3. Create src/providers/QueryProvider.tsx:
       ```tsx
       import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
       import type { ReactNode } from 'react';

       const queryClient = new QueryClient({
         defaultOptions: {
           queries: {
             staleTime: Infinity,
             refetchOnWindowFocus: false,
             retry: 1,
             gcTime: 10 * 60 * 1000,
           },
         },
       });

       export function QueryProvider({ children }: { children: ReactNode }) {
         return <QueryClientProvider client={queryClient}>{children}</QueryClientProvider>;
       }
       ```

    4. Create src/components/charts/nivoTheme.ts — shared Nivo dark theme:
       ```ts
       export const nivoTheme = {
         background: '#0f172a',
         text: { fill: '#94a3b8', fontSize: 12 },
         axis: {
           ticks: {
             line: { stroke: '#334155' },
             text: { fill: '#64748b', fontSize: 12 },
           },
           legend: { text: { fill: '#64748b', fontSize: 12 } },
           domain: { line: { stroke: '#334155' } },
         },
         grid: { line: { stroke: '#1e293b', strokeWidth: 1 } },
         legends: { text: { fill: '#94a3b8' } },
         tooltip: {
           container: {
             background: '#1e293b',
             color: '#f1f5f9',
             border: '1px solid #334155',
             fontSize: 12,
           },
         },
         crosshair: { line: { stroke: '#64748b' } },
       };
       ```

    5. Create src/components/layout/Header.tsx:
       - "Tennis Quant" wordmark on left (20px/600, slate-100)
       - Refresh button on right using shadcn/ui Button + lucide-react RefreshCw icon
       - Background: slate-900, border-bottom: slate-700 1px
       - Button text: "Refresh Data", onClick is a no-op prop for now (wired in Plan 05)
       - Refresh button: `aria-label="Refresh Data"`

    6. Create src/components/layout/TabNav.tsx:
       - Uses shadcn/ui Tabs component (Tabs, TabsList, TabsTrigger, TabsContent)
       - 4 tabs: "Overview", "Backtest", "Models", "Signals"
       - defaultValue="overview"
       - Active tab: green-500 bottom border (via className override on TabsTrigger)
       - Tab trigger height: 44px, spacing between: 24px (lg)
       - Each TabsContent wraps the corresponding tab component

    7. Create 4 tab stub components (src/tabs/OverviewTab.tsx, BacktestTab.tsx, ModelsTab.tsx, SignalsTab.tsx):
       - Each renders: `<div className="p-6"><h2 className="text-xl font-semibold text-slate-100">{Tab Name}</h2><p className="text-slate-500 mt-2">Content coming soon.</p></div>`

    8. Create src/App.tsx:
       ```tsx
       import { QueryProvider } from './providers/QueryProvider';
       import { Header } from './components/layout/Header';
       import { TabNav } from './components/layout/TabNav';
       import { Toaster } from '@/components/ui/sonner';

       function App() {
         return (
           <QueryProvider>
             <div className="min-h-screen bg-background text-foreground">
               <Header />
               <TabNav />
               <Toaster />
             </div>
           </QueryProvider>
         );
       }

       export default App;
       ```

    9. Update src/main.tsx to import App (ensure dark class is set).

    10. Create src/__tests__/App.test.tsx:
        ```tsx
        import { render, screen } from '@testing-library/react';
        import { describe, it, expect } from 'vitest';
        import App from '../App';

        describe('App', () => {
          it('renders the Tennis Quant header', () => {
            render(<App />);
            expect(screen.getByText('Tennis Quant')).toBeInTheDocument();
          });

          it('renders all 4 tab triggers', () => {
            render(<App />);
            expect(screen.getByRole('tab', { name: /overview/i })).toBeInTheDocument();
            expect(screen.getByRole('tab', { name: /backtest/i })).toBeInTheDocument();
            expect(screen.getByRole('tab', { name: /models/i })).toBeInTheDocument();
            expect(screen.getByRole('tab', { name: /signals/i })).toBeInTheDocument();
          });

          it('shows Overview tab content by default', () => {
            render(<App />);
            expect(screen.getByText('Overview')).toBeInTheDocument();
          });
        });
        ```
  </action>
  <verify>
    <automated>cd dashboard && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - dashboard/src/api/client.ts contains `const BASE_URL = 'http://localhost:8000/api/v1'`
    - dashboard/src/api/client.ts exports apiFetch function
    - dashboard/src/api/types.ts exports BankrollResponse, BacktestSummary, PredictionRow, ModelMetrics, CalibrationResponse, CalibrationBin, JobResponse, RefreshStatusResponse
    - dashboard/src/providers/QueryProvider.tsx contains staleTime: Infinity
    - dashboard/src/providers/QueryProvider.tsx contains refetchOnWindowFocus: false
    - dashboard/src/components/charts/nivoTheme.ts contains background: '#0f172a'
    - dashboard/src/components/layout/Header.tsx contains "Tennis Quant"
    - dashboard/src/components/layout/Header.tsx contains "Refresh Data"
    - dashboard/src/components/layout/TabNav.tsx contains TabsTrigger
    - dashboard/src/components/layout/TabNav.tsx contains "overview"
    - dashboard/src/App.tsx contains QueryProvider
    - dashboard/src/App.tsx contains Header
    - dashboard/src/App.tsx contains TabNav
    - dashboard/src/App.tsx contains Toaster
    - `cd dashboard && npx vitest run` exits 0 with 3 passing tests
    - `cd dashboard && npm run build` exits 0
  </acceptance_criteria>
  <done>API client with typed fetch wrapper, TypeScript types mirroring all Pydantic schemas, QueryProvider with manual-refresh config, Nivo dark theme, app shell with Header + 4-tab navigation, and 3 passing smoke tests</done>
</task>

</tasks>

<verification>
- `cd dashboard && npm run build` exits 0 (no TypeScript errors)
- `cd dashboard && npx vitest run --reporter=verbose` shows 3 passing tests
- `cd dashboard && npm run dev` starts Vite dev server (manual check)
- package.json contains all locked dependencies at correct version ranges
</verification>

<success_criteria>
- Vite dev server starts and renders dark-themed dashboard with 4 navigable tabs
- All TypeScript types match src/api/schemas.py Pydantic models
- API client configured for http://localhost:8000/api/v1
- TanStack Query configured with staleTime: Infinity and refetchOnWindowFocus: false
- Vitest passes all smoke tests
- Build succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-react-dashboard-core/06-01-SUMMARY.md`
</output>
